service messagebox
{
        disable         = no
	socket_type	= stream
	protocol	= tcp
	user		= messagebox 
	wait		= no
        type            = UNLISTED
        bind            = 0.0.0.0
        port            = 5050
        server          = /home/messagebox/run.sh
        instances       = 50
}
